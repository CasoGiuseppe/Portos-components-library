
export type UniqueId = string;

export enum Types {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    TERTIARY = 'tertiary',
}

export enum Variants {
    ALT = 'ALT',
}

export enum Sizes {
    M = 'M',
    L = 'L',
    S = 'S'
}